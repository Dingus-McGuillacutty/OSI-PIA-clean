// Historical constraint location.
//
// Do not execute this file. The former statements used an `id` property that
// is not the stable identity of the implemented OSI reference graph.
//
// Current executable constraints are created by ordered files in
// `graph/migrations/`. OSI structural entities use `Entity.entity_id`; PIA
// contracted objects use participant_id, source_id, experience_id,
// evidence_id, capability_id, and the additional object-specific identities
// declared by their migrations.
